query MetricReport($input: MetricReportInput!) {
  metricReport(input: $input) {
    pageInfo {
      startCursor
      endCursor
      hasNextPage
      hasPreviousPage
    }
    headers
    rows
  }
}
